{
  "name": "bank-server",
  "version": "1.0.0",
  "description": "A REST web server for a bank",
  "main": "app.js",
  "scripts": {
    "create-db": "psql -tc \"SELECT 1 FROM pg_database WHERE datname = '$DATABASE_NAME'\" | grep -q 1 || psql -c \"CREATE DATABASE $DATABASE_NAME\"",
    "test": "jest --coverage",
    "coverage": "nyc report --temp-directory=coverage --reporter=text-lcov | coveralls",
    "clean": "rimraf dist",
    "build": "babel app  --ignore app/test --copy-files --out-dir dist",
    "lint": "eslint app",
    "start": "npm run clean && npm run build && npm run create-db && npm run migrate && node ./dist/bin/start",
    "start-dev": "nodemon ./app/bin/start --exec babel-node",
    "migrate": "sequelize db:migrate",
    "migrate-undo": "sequelize db:migrate:undo"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dsullivan7/bank-server.git"
  },
  "private": true,
  "license": "UNLICENSED",
  "author": "Danny Sullivan <dbsullivan23@gmail.com>",
  "keywords": [],
  "bugs": {
    "url": "https://github.com/dsullivan7/bank-server/issues"
  },
  "homepage": "https://github.com/dsullivan7/bank-server#readme",
  "engines": {
    "node": "8.1.4"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "eslint": "^3.19.0",
    "eslint-config-airbnb-base": "^11.1.3",
    "eslint-plugin-import": "^2.2.0",
    "jest": "^20.0.4",
    "nyc": "^11.1.0",
    "supertest": "^3.0.0",
    "umzug": "^2.0.1"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-polyfill": "^6.23.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-2": "^6.22.0",
    "babel-register": "^6.24.0",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "express-bearer-token": "^2.1.0",
    "google-auth-library": "^0.10.0",
    "morgan": "^1.8.2",
    "pg": "^6.4.1",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.3.2"
  }
}
